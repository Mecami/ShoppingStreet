<!--components/my-goods/my-goods.wxml-->
<view class="goods">
    <block wx:for="{{typeList}}" wx:key="{{index}}">
         <my-goods-item item="{{item}}" class="home-goods-item"/>
    </block>
</view> 
